# from django import template

# register = template.Library()

# @register.filter(name='replace')
# def replace(value, arg):
#     """
#     Replaces all occurrences of the first argument in a string with the second
#     argument.
#     """


#     return value[0].['value'](arg, '')